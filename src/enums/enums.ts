export enum Status {
  All = 'All',
  Active = 'Active',
  Completed = 'Completed',
}

export enum ErrorType {
  Add = 'Add',
  EmptyTitle = 'EmptyTitle',
  Delete = 'Delete',
  Update = 'Update',
  None = 'None',
}
