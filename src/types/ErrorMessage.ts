export enum ErrorMessage {
  Load = 'Failed in loading todos',
  Add = 'Failed in adding todos',
  Delete = 'Failed in deleting todos',
  Update = 'Failed in updating todos',
  Title = 'Title can not be empty',
}
