export const USER_ID = 10642;

export enum FilterValues {
  ALL = 'all',
  ACTIVE = 'active',
  COMPLETED = 'completed',
}

export enum ErrorTypes {
  ADD = 'add',
  DELETE = 'delete',
  UPLOAD = 'upload',
  UPDATE = 'update',
}
