export enum ACTIONS {
  SORT,
  SET_LENGTH,
  SET_LIST,
  SET_ERROR,
  DELETE,
  ADD,
  SET_LOADING,
  TOGGLE_ALL,
}

export enum FILTER {
  ALL = 'All',
  ACTIVE = 'Active',
  COMPLETED = 'Completed',
}

export const USER_ID = 11384;
